# Core ML / RL dependencies
gymnasium>=0.29.0
numpy>=1.26.0
matplotlib>=3.7.0
pandas>=2.0.0
seaborn>=0.12.0
scipy>=1.10.0
pyyaml>=6.0
tensorboard>=2.13.0

# PyTorch 2.8.0 with CUDA 12.9 (installed separately with custom index)
# Install with: pip install torch==2.8.0+cu129 torchvision==0.23.0+cu129 torchaudio==2.8.0+cu129 --index-url https://download.pytorch.org/whl/cu129
# torch==2.8.0+cu129
# torchvision==0.23.0+cu129
# torchaudio==2.8.0+cu129